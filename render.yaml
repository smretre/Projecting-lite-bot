services:
  - type: web
    name: telegram-baas-bot
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: MP_ACCESS_TOKEN
        sync: false
      - key: PUBLIC_URL
        sync: false
